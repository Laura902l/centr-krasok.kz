# Центр красок #1 — Telegram Bot

AI-ассистент для компании centr-krasok.kz

## Запуск

1. Установить зависимости: `pip install -r requirements.txt`
2. Создать `.env` файл с переменными:
   - `TELEGRAM_BOT_TOKEN`
   - `GEMINI_API_KEY`
3. Запустить: `python bot.py`
